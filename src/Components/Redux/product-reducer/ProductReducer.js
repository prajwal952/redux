// condition we will be writing if the user clicks on btn wts the condition that needs to be happening

const initailState={
    products:[],
    cartData:[]
}

const productReducer = (state = initailState,action)=>{

}